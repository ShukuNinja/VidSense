# Web backend dependencies (Phase 1). Install alongside requirements.txt:
#   pip install -r requirements-backend.txt
fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0

# Auth (multi-user)
bcrypt>=4.0
PyJWT>=2.8

# Hosted LLM provider (optional; used when LLM_PROVIDER=openai)
openai>=1.30
